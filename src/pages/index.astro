---
const site = {
  name: "Amicbridge",
  tagline: "Borrowing & lending — made human, simple, and transparent.",
  domain: "amicbridge.com",
};

const borrowerBenefits = [
  "Request funding from real people — not only banks.",
  "Explain your story and terms in your own words.",
  "Get flexible offers and negotiate directly.",
];

const investorBenefits = [
  "Review real borrowers and use-cases — not just numbers.",
  "Set your own return target and conditions.",
  "Choose your risk level and deal size.",
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{site.name} — Simple borrowing & lending</title>
    <meta name="description" content="Amicbridge is a people-first borrowing and lending platform. Send a borrowing request or an investment request in minutes." />
    <meta name="robots" content="index,follow" />
  </head>

  <body>
    <!-- Top bar -->
    <header class="topbar">
      <div class="container topbar-inner">
        <a class="brand" href="#top" aria-label={`${site.name} home`}>
          <span class="logo">A</span>
          <span class="brand-text">
            <span class="brand-name">{site.name}</span>
            <span class="brand-sub">{site.domain}</span>
          </span>
        </a>

        <nav class="nav" aria-label="Primary navigation">
          <a href="#how">How it works</a>
          <a href="#forms">Send request</a>
          <a class="nav-cta" href="#invest">Investment</a>
        </nav>
      </div>
    </header>

    <main id="top">
      <!-- Hero -->
      <section class="hero">
        <div class="container hero-grid">
          <div class="hero-copy">
            <p class="badge">Early access • Requests open</p>

            <h1>
              Borrow from people.<br />
              <span>Lend (invest) in people.</span><br />
              Simple. Direct. Clear.
            </h1>

            <p class="lead">
              {site.name} is a connection platform for borrowing and lending. You choose the terms — we help you
              send a request and find the right match.
            </p>

            <div class="cta-row">
              <a class="btn btn-primary" href="#borrow">Send borrowing request</a>
              <a class="btn btn-outline" href="#invest">Send investment request</a>
            </div>

            <p class="note">
              <strong>Important:</strong> Amicbridge does not provide loans, does not hold funds, and does not
              guarantee repayments. Always do your own checks.
            </p>

            <div class="mini-grid" aria-label="Highlights">
              <div class="mini-card">
                <strong>Fast</strong>
                <span>Send a request in minutes.</span>
              </div>
              <div class="mini-card">
                <strong>Flexible</strong>
                <span>Set amount, duration, and target rate.</span>
              </div>
              <div class="mini-card">
                <strong>Human</strong>
                <span>Explain your situation or strategy.</span>
              </div>
            </div>
          </div>

          <aside class="hero-panel" aria-label="Preview">
            <div class="panel">
              <div class="panel-title">
                <span class="dot" aria-hidden="true"></span>
                Example preview
              </div>

              <div class="panel-block">
                <div class="label">Borrow request</div>
                <div class="row">
                  <div class="chip"><span>Amount</span><strong>€2,000</strong></div>
                  <div class="chip"><span>Duration</span><strong>6 months</strong></div>
                </div>
                <div class="chip full"><span>Max APR</span><strong>5%</strong></div>
              </div>

              <div class="divider" aria-hidden="true"></div>

              <div class="panel-block">
                <div class="label">Investor offer</div>
                <div class="chip full"><span>Offer</span><strong>€2,000 at 4.5% APR</strong></div>
                <div class="row">
                  <div class="chip"><span>Type</span><strong>Personal</strong></div>
                  <div class="chip"><span>Risk</span><strong>Medium</strong></div>
                </div>
              </div>

              <p class="panel-note">
                Example only. Real matching, messaging, and onboarding will be added as the platform evolves.
              </p>
            </div>
          </aside>
        </div>
      </section>

      <!-- How it works -->
      <section class="section" id="how">
        <div class="container">
          <div class="section-head">
            <h2>How it works</h2>
            <p>
              Amicbridge is built around one idea: a simple request creates clarity. Borrowers and investors can
              meet on understandable terms.
            </p>
          </div>

          <div class="steps">
            <div class="step">
              <div class="step-num">1</div>
              <div>
                <strong>Send a request</strong>
                <p>Borrowers request funding. Investors describe their investment strategy and conditions.</p>
              </div>
            </div>
            <div class="step">
              <div class="step-num">2</div>
              <div>
                <strong>Review and connect</strong>
                <p>We review incoming requests and help connect relevant matches (manual during early access).</p>
              </div>
            </div>
            <div class="step">
              <div class="step-num">3</div>
              <div>
                <strong>Agree terms directly</strong>
                <p>You choose: proceed, negotiate, or decline. The decision is always yours.</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Benefits -->
      <section class="section alt">
        <div class="container two-col">
          <div class="card">
            <h3>For borrowers</h3>
            <ul>
              {borrowerBenefits.map((b) => <li>{b}</li>)}
            </ul>
          </div>

          <div class="card">
            <h3>For investors / lenders</h3>
            <ul>
              {investorBenefits.map((b) => <li>{b}</li>)}
            </ul>
          </div>
        </div>
      </section>

      <!-- Forms -->
      <section class="section" id="forms">
        <div class="container">
          <div class="section-head">
            <h2>Send a request</h2>
            <p>
              Choose the form you need. Both go to the same secure inbox via Formspree.
            </p>
          </div>

          <div class="forms-grid">
            <!-- Borrow request -->
            <article class="form-card" id="borrow">
              <h3>Borrowing request</h3>
              <p class="muted">Tell us what you need and the terms you can handle.</p>

              <form action="https://formspree.io/f/xzznjkvd" method="POST">
                <input type="hidden" name="requestType" value="Borrowing request" />

                <div class="grid2">
                  <label>
                    Full name
                    <input name="name" type="text" placeholder="Your name" required />
                  </label>
                  <label>
                    Email
                    <input name="email" type="email" placeholder="you@email.com" required />
                  </label>
                </div>

                <div class="grid3">
                  <label>
                    Amount (EUR)
                    <input name="amount" type="number" min="0" step="100" placeholder="2000" required />
                  </label>
                  <label>
                    Duration (months)
                    <input name="durationMonths" type="number" min="1" step="1" placeholder="6" required />
                  </label>
                  <label>
                    Max APR (%)
                    <input name="maxApr" type="number" min="0" step="0.1" placeholder="5" />
                  </label>
                </div>

                <label>
                  Purpose / situation
                  <textarea
                    name="message"
                    rows="5"
                    placeholder="Explain what the loan is for, your situation, and any important details."
                    required></textarea>
                </label>

                <label class="checkbox">
                  <input type="checkbox" name="acceptDisclaimer" required />
                  <span>I understand Amicbridge does not provide loans or guarantee repayment.</span>
                </label>

                <button class="btn btn-primary" type="submit">Send borrowing request</button>
                <p class="tiny">By submitting, you agree to be contacted about your request.</p>
              </form>
            </article>

            <!-- Investment request -->
            <article class="form-card" id="invest">
              <h3>Investment request</h3>
              <p class="muted">Share your strategy and what kinds of borrowers you want to support.</p>

              <form action="https://formspree.io/f/xzznjkvd" method="POST">
                <input type="hidden" name="requestType" value="Investment request" />

                <div class="grid2">
                  <label>
                    Full name
                    <input name="name" type="text" placeholder="Your name" required />
                  </label>
                  <label>
                    Email
                    <input name="email" type="email" placeholder="you@email.com" required />
                  </label>
                </div>

                <div class="grid3">
                  <label>
                    Budget (EUR)
                    <input name="budget" type="number" min="0" step="100" placeholder="5000" required />
                  </label>
                  <label>
                    Target APR (%)
                    <input name="targetApr" type="number" min="0" step="0.1" placeholder="6" />
                  </label>
                  <label>
                    Preferred duration (months)
                    <input name="preferredDurationMonths" type="number" min="1" step="1" placeholder="6–24" />
                  </label>
                </div>

                <label>
                  Investment strategy / conditions
                  <textarea
                    name="strategy"
                    rows="5"
                    placeholder="Example: personal loans only, max 12 months, prefer stable income, require basic verification, etc."
                    required></textarea>
                </label>

                <label>
                  Notes (optional)
                  <textarea name="notes" rows="3" placeholder="Any extra details."></textarea>
                </label>

                <label class="checkbox">
                  <input type="checkbox" name="acceptDisclaimer" required />
                  <span>I understand lending involves risk and Amicbridge does not guarantee repayments.</span>
                </label>

                <button class="btn btn-primary" type="submit">Send investment request</button>
                <p class="tiny">By submitting, you agree to be contacted about your request.</p>
              </form>
            </article>
          </div>
        </div>
      </section>

      <!-- Footer -->
      <footer class="footer" id="safety">
        <div class="container footer-inner">
          <div>
            <div class="foot-brand">
              <span class="logo small" aria-hidden="true">A</span>
              <strong>{site.name}</strong>
            </div>
            <p class="muted" style="margin-top: 0.5rem;">
              Connection platform only. We do not provide loans, hold money, or guarantee repayments.
            </p>
          </div>

          <div class="foot-links">
            <a href="#how">How it works</a>
            <a href="#borrow">Borrow</a>
            <a href="#invest">Invest</a>
          </div>
        </div>

        <div class="container foot-bottom">
          <p class="tiny">© {new Date().getFullYear()} {site.name}. All rights reserved.</p>
        </div>
      </footer>
    </main>

    <style>
      :root{
        --bg:#ffffff;
        --text:#0f172a;
        --muted:#475569;
        --muted2:#64748b;
        --border:#e2e8f0;
        --soft:#f8fafc;
        --soft2:#f1f5f9;
        --accent:#2563eb;
        --accent2:#1d4ed8;
        --shadow:0 16px 50px rgba(2, 6, 23, 0.08);
        --r:16px;
      }
      *{box-sizing:border-box}
      html,body{margin:0;padding:0}
      body{
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial;
        background: var(--bg);
        color: var(--text);
        line-height: 1.5;
      }
      a{color:inherit}
      .container{width:min(1120px,calc(100% - 2rem));margin:0 auto}

      /* Topbar */
      .topbar{
        position: sticky; top: 0; z-index: 20;
        background: rgba(255,255,255,.88);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--border);
      }
      .topbar-inner{
        display:flex;align-items:center;justify-content:space-between;
        padding: .95rem 0;
        gap: 1rem;
      }
      .brand{
        display:flex;align-items:center;gap:.7rem;
        text-decoration:none;
      }
      .logo{
        width:36px;height:36px;border-radius:12px;
        display:grid;place-items:center;
        background: linear-gradient(135deg, var(--accent), #22c55e);
        color:#fff;font-weight:800;
        box-shadow: 0 10px 25px rgba(37, 99, 235, 0.20);
      }
      .logo.small{width:30px;height:30px;border-radius:10px}
      .brand-text{display:flex;flex-direction:column;line-height:1.1}
      .brand-name{font-weight:800}
      .brand-sub{font-size:.86rem;color:var(--muted2)}
      .nav{display:flex;gap:1rem;align-items:center}
      .nav a{
        text-decoration:none;
        color: var(--muted);
        font-weight:600;
        padding:.35rem .45rem;
        border-radius: 10px;
      }
      .nav a:hover{background: var(--soft2); color: var(--text)}
      .nav-cta{
        border:1px solid var(--border);
        background: var(--soft);
        color: var(--text) !important;
      }

      /* Hero */
      .hero{padding: 3.6rem 0 2.4rem}
      .hero-grid{
        display:grid;
        grid-template-columns: 1.1fr .9fr;
        gap: 2rem;
        align-items: start;
      }
      .badge{
        display:inline-flex;align-items:center;
        font-size:.9rem;
        color: var(--muted);
        background: var(--soft);
        border:1px solid var(--border);
        padding:.35rem .6rem;
        border-radius: 999px;
        margin: 0;
      }
      h1{
        margin: .9rem 0 0;
        font-size: clamp(2.1rem, 4vw, 3.1rem);
        line-height: 1.07;
        letter-spacing: -0.02em;
      }
      h1 span{
        background: linear-gradient(90deg, var(--accent), #16a34a);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
      }
      .lead{margin: 1rem 0 1.2rem; color: var(--muted); font-size: 1.05rem; max-width: 62ch;}
      .cta-row{display:flex;gap:.8rem;flex-wrap:wrap; margin: .6rem 0 1.1rem;}
      .btn{
        display:inline-flex;align-items:center;justify-content:center;
        padding:.82rem 1.05rem;
        border-radius: 999px;
        font-weight:800;
        border:1px solid var(--border);
        background: var(--soft);
        color: var(--text);
        text-decoration:none;
        cursor:pointer;
      }
      .btn:hover{filter:brightness(0.98)}
      .btn-primary{
        background: var(--accent);
        border-color: var(--accent);
        color: #fff;
        box-shadow: 0 14px 30px rgba(37,99,235,.18);
      }
      .btn-primary:hover{background: var(--accent2); border-color: var(--accent2)}
      .btn-outline{background:#fff}
      .note{
        margin: 0.9rem 0 0;
        font-size: .95rem;
        color: var(--muted);
        background: var(--soft);
        border: 1px solid var(--border);
        padding: .85rem 1rem;
        border-radius: var(--r);
      }

      .mini-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:.8rem;margin-top:1.1rem}
      .mini-card{
        border: 1px solid var(--border);
        background: #fff;
        border-radius: var(--r);
        padding: .9rem 1rem;
      }
      .mini-card strong{display:block}
      .mini-card span{display:block;color:var(--muted);margin-top:.25rem;font-size:.95rem}

      /* Right panel */
      .panel{
        border:1px solid var(--border);
        background: #fff;
        border-radius: var(--r);
        padding: 1.05rem;
        box-shadow: var(--shadow);
      }
      .panel-title{
        display:flex;align-items:center;gap:.5rem;
        font-weight:800;color:var(--muted);
        margin-bottom: .9rem;
      }
      .dot{width:10px;height:10px;border-radius:999px;background:#22c55e; box-shadow:0 0 0 6px rgba(34,197,94,.12)}
      .label{
        font-size:.78rem;
        text-transform: uppercase;
        letter-spacing: .14em;
        color: var(--muted2);
        margin-bottom: .35rem;
      }
      .panel-block{margin-bottom: .9rem}
      .row{display:grid;grid-template-columns: 1fr 1fr; gap:.6rem}
      .chip{
        border:1px solid var(--border);
        background: var(--soft);
        border-radius: 14px;
        padding: .7rem .75rem;
        display:flex;align-items:center;justify-content:space-between; gap:.8rem;
      }
      .chip span{color: var(--muted); font-weight: 650; font-size:.92rem}
      .chip strong{font-weight:900}
      .chip.full{margin-top:.6rem}
      .divider{height:1px;background: var(--border); margin: 1rem 0}
      .panel-note{margin:0;color:var(--muted);font-size:.92rem}

      /* Sections */
      .section{padding: 2.8rem 0}
      .section.alt{background: var(--soft)}
      .section-head{max-width: 75ch; margin-bottom: 1.2rem;}
      .section-head h2{margin:0;font-size: 1.7rem}
      .section-head p{margin:.55rem 0 0; color: var(--muted);}

      .steps{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem}
      .step{
        background:#fff;
        border: 1px solid var(--border);
        border-radius: var(--r);
        padding: 1rem 1.05rem;
        display:flex;gap:.85rem;
      }
      .step-num{
        width:34px;height:34px;border-radius:12px;
        display:grid;place-items:center;
        background: rgba(37,99,235,.10);
        border:1px solid rgba(37,99,235,.22);
        color: var(--accent);
        font-weight: 900;
        flex: 0 0 auto;
      }
      .step p{margin:.35rem 0 0; color: var(--muted);}

      .two-col{display:grid;grid-template-columns:1fr 1fr;gap:1rem}
      .card{
        background:#fff;
        border:1px solid var(--border);
        border-radius: var(--r);
        padding: 1.1rem 1.2rem;
        box-shadow: 0 8px 30px rgba(2,6,23,.04);
      }
      .card h3{margin:0 0 .6rem}
      ul{margin:0;padding-left:1.2rem;color:var(--muted)}
      li{margin:.5rem 0}

      /* Forms */
      .forms-grid{display:grid;grid-template-columns:1fr 1fr;gap:1rem;align-items:start}
      .form-card{
        background:#fff;
        border:1px solid var(--border);
        border-radius: var(--r);
        padding: 1.2rem;
        box-shadow: 0 10px 40px rgba(2,6,23,.05);
      }
      .form-card h3{margin:0 0 .35rem}
      .muted{color: var(--muted)}
      form{margin-top: .9rem}
      label{display:block;font-weight:750;margin:.75rem 0 .25rem}
      input, textarea{
        width: 100%;
        border:1px solid var(--border);
        background: #fff;
        border-radius: 14px;
        padding: .78rem .85rem;
        font: inherit;
        color: var(--text);
        outline: none;
      }
      textarea{resize: vertical}
      input:focus, textarea:focus{
        border-color: rgba(37,99,235,.45);
        box-shadow: 0 0 0 4px rgba(37,99,235,.12);
      }
      .grid2{display:grid;grid-template-columns:1fr 1fr;gap:.8rem}
      .grid3{display:grid;grid-template-columns:repeat(3,1fr);gap:.8rem}
      .checkbox{display:flex;gap:.6rem;align-items:flex-start;font-weight:650;color:var(--muted);margin-top:.8rem}
      .checkbox input{width:auto;margin-top:.2rem}
      .tiny{margin:.6rem 0 0;color:var(--muted2);font-size:.88rem}

      /* Footer */
      .footer{border-top:1px solid var(--border); padding: 2.2rem 0 1.6rem; background:#fff}
      .footer-inner{display:flex;align-items:flex-start;justify-content:space-between;gap:1rem;flex-wrap:wrap}
      .foot-brand{display:flex;align-items:center;gap:.6rem}
      .foot-links{display:flex;gap:1rem;flex-wrap:wrap}
      .foot-links a{color:var(--muted);font-weight:650;text-decoration:none}
      .foot-links a:hover{text-decoration:underline}
      .foot-bottom{margin-top:1.2rem;padding-top:1rem;border-top:1px solid var(--border)}
      .tiny{color:var(--muted2)}

      /* Responsive */
      @media (max-width: 980px){
        .hero-grid{grid-template-columns:1fr}
        .steps{grid-template-columns:1fr}
        .two-col{grid-template-columns:1fr}
        .forms-grid{grid-template-columns:1fr}
        .grid3{grid-template-columns:1fr}
      }
    </style>
  </body>
</html>
