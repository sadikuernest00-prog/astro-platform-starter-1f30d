const featuresBorrower = [
  "Reach real people willing to lend, not just banks.",
  "Explain your situation in your own words.",
  "Negotiate repayment terms directly with lenders.",
];

const featuresLender = [
  "See real people and projects, not just numbers.",
  "Set your own interest rate and conditions.",
  "Chat with borrowers before you commit.",
];

export default function AmicbridgeLanding() {
  return (
    <main>
      {/* HERO */}
      <section className="hero" id="start">
        <div>
          <h1>
            Borrow from people. <span>Lend to people.</span>
            <br />
            One simple bridge.
          </h1>

          <p className="hero-text">
            Amicbridge connects people who want to{" "}
            <strong>borrow</strong> with people who want to{" "}
            <strong>lend</strong>. You agree the terms – we just help you find
            each other.
          </p>

          <div className="cta-row">
            <button className="btn btn-primary">I want to borrow</button>
            <button className="btn btn-outline">I want to lend</button>
          </div>

          <p className="tiny-note">
            Accounts, messaging and full matching are coming soon. For now, you
            can join the early access list.
          </p>
        </div>

        {/* PREVIEW CARD */}
        <aside>
          <div className="card">
            <div className="pill">
              <span className="pill-dot"></span>
              Live preview · Example match
            </div>

            <p className="inline-label">Borrower</p>

            <div className="fake-input">
              <span>Amount</span>
              <strong>€2,000</strong>
            </div>

            <div className="input-row">
              <div className="fake-input">
                <span>Duration</span>
                <strong>6 months</strong>
              </div>
              <div className="fake-input">
                <span>Max interest</span>
                <strong>5%</strong>
              </div>
            </div>

            <p className="inline-label">Lender</p>

            <div className="fake-input">
              <span>Offer</span>
              <strong>€2,000 · 4.5% APR</strong>
            </div>

            <div className="toggle-row">
              <span className="toggle-chip active">Personal loan</span>
              <span className="toggle-chip">Small business</span>
            </div>

            <p className="tiny-note" style={{ marginTop: "0.8rem" }}>
              Example only. On the real platform you’ll be able to post your own
              request or lending offer and talk directly to matches.
            </p>
          </div>
        </aside>
      </section>

      {/* FEATURES */}
      <section className="features" id="how">
        <div className="feature-card">
          <h2>For borrowers</h2>
          <ul>
            {featuresBorrower.map((item, i) => (
              <li key={i}>{item}</li>
            ))}
          </ul>
        </div>

        <div className="feature-card">
          <h2>For lenders</h2>
          <ul>
            {featuresLender.map((item, i) => (
              <li key={i}>{item}</li>
            ))}
          </ul>
        </div>
      </section>

      {/* FOOTER / SAFETY */}
      <footer id="safety">
        <p>
          <strong>Important:</strong> Amicbridge is a connection platform. We do
          not provide loans, hold money, or guarantee repayments. Always do your
          own checks and consult a professional if needed before lending or
          borrowing.
        </p>
      </footer>
    </main>
  );
}
