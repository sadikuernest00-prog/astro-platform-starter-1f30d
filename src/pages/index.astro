---
// src/pages/index.astro
const siteName = "AmicBridge";
const tagline = "A simple matching platform for lenders, borrowers, and profit-sharing collaborations.";
const formspreeAction = "https://formspree.io/f/xzznjkvd";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{siteName} — Matching platform</title>
    <meta
      name="description"
      content="AmicBridge is a simple matching platform where lenders, borrowers, and co-investors can find each other, discuss terms, and create private agreements."
    />
    <style>
      :root{
        --bg:#ffffff;
        --text:#0f172a;
        --muted:#475569;
        --border:#e2e8f0;
        --soft:#f8fafc;
        --accent:#0f172a;
        --shadow: 0 1px 2px rgba(0,0,0,.06), 0 6px 18px rgba(0,0,0,.06);
        --radius: 14px;
        --max: 980px;
      }

      *{box-sizing:border-box}
      html,body{margin:0;padding:0}
      body{
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
        background: var(--bg);
        color: var(--text);
        line-height: 1.55;
      }

      a{color:inherit; text-decoration:none}
      a:hover{text-decoration:underline}
      .container{max-width: var(--max); margin: 0 auto; padding: 0 18px}
      .row{display:flex; align-items:center; gap:14px}
      .spacer{height:26px}

      header{
        position: sticky;
        top:0;
        z-index:10;
        background: rgba(255,255,255,.92);
        backdrop-filter: blur(8px);
        border-bottom: 1px solid var(--border);
      }
      .nav{
        display:flex;
        align-items:center;
        justify-content:space-between;
        padding: 16px 0;
      }
      .brand{
        display:flex;
        flex-direction:column;
        gap:2px;
      }
      .brand strong{font-size:18px; letter-spacing:.2px}
      .brand span{font-size:13px; color: var(--muted)}
      nav ul{
        list-style:none;
        display:flex;
        gap:18px;
        margin:0;
        padding:0;
        flex-wrap:wrap;
      }
      nav a{
        font-size:14px;
        color: var(--muted);
        padding: 6px 8px;
        border-radius: 10px;
      }
      nav a:hover{
        background: var(--soft);
        color: var(--text);
        text-decoration:none;
      }

      main{padding: 44px 0 70px}

      h1{
        font-size: clamp(34px, 4vw, 52px);
        margin: 0 0 10px 0;
        letter-spacing: -0.6px;
      }
      p.lead{
        font-size: 18px;
        color: var(--muted);
        max-width: 70ch;
        margin: 0 0 18px 0;
      }

      .callout{
        border: 1px solid var(--border);
        background: var(--soft);
        border-radius: var(--radius);
        padding: 16px 16px;
        color: var(--muted);
      }
      .callout strong{color: var(--text)}

      .actions{
        display:flex;
        gap:12px;
        flex-wrap:wrap;
        margin: 18px 0 12px 0;
      }
      .btn{
        display:inline-flex;
        align-items:center;
        justify-content:center;
        border-radius: 14px;
        padding: 12px 16px;
        border: 1px solid var(--border);
        background: #fff;
        color: var(--text);
        font-weight: 600;
        font-size: 14px;
      }
      .btn.primary{
        background: var(--accent);
        border-color: var(--accent);
        color:#fff;
        box-shadow: var(--shadow);
      }
      .btn:hover{
        text-decoration:none;
        filter: brightness(.98);
      }

      .bullets{
        margin: 14px 0 0 0;
        padding: 0 0 0 18px;
        color: var(--muted);
      }
      .bullets li{margin: 6px 0}
      .bullets b{color: var(--text)}

      section{
        margin-top: 48px;
        padding-top: 6px;
      }
      section h2{
        font-size: 26px;
        margin: 0 0 10px 0;
        letter-spacing: -0.3px;
      }
      section p{
        color: var(--muted);
        max-width: 80ch;
        margin: 0 0 12px 0;
      }

      .grid{
        display:grid;
        grid-template-columns: 1fr;
        gap: 14px;
      }
      @media (min-width: 860px){
        .grid{grid-template-columns: 1.15fr .85fr}
      }

      .card{
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 16px;
        background: #fff;
      }

      .steps{
        display:grid;
        grid-template-columns: 1fr;
        gap: 10px;
        margin-top: 10px;
      }
      .step{
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 12px 12px;
        background: #fff;
      }
      .step .kicker{
        font-size: 12px;
        color: var(--muted);
        text-transform: uppercase;
        letter-spacing: .08em;
        margin-bottom: 6px;
      }
      .step strong{display:block; margin-bottom: 4px}

      form{
        display:grid;
        gap: 10px;
      }
      label{
        font-size: 13px;
        color: var(--muted);
        display:block;
        margin-bottom: 4px;
      }
      input, textarea, select{
        width:100%;
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 10px 12px;
        font-size: 14px;
        background:#fff;
        color: var(--text);
      }
      textarea{min-height: 120px; resize: vertical}
      .form-row{
        display:grid;
        grid-template-columns: 1fr;
        gap: 10px;
      }
      @media (min-width: 640px){
        .form-row{grid-template-columns: 1fr 1fr}
      }

      footer{
        border-top: 1px solid var(--border);
        padding: 22px 0;
        color: var(--muted);
        font-size: 13px;
      }
      .fineprint{max-width: 95ch}
      .anchor{scroll-margin-top: 84px}
    </style>
  </head>

  <body>
    <header>
      <div class="container">
        <div class="nav">
          <div class="brand">
            <strong>{siteName}</strong>
            <span>amicbridge.com</span>
          </div>
          <nav aria-label="Primary navigation">
            <ul>
              <li><a href="#about">About</a></li>
              <li><a href="#how">How it works</a></li>
              <li><a href="#request">Send request</a></li>
              <li><a href="#contact">Contact</a></li>
            </ul>
          </nav>
        </div>
      </div>
    </header>

    <main class="container">
      <section class="anchor" id="top">
        <h1>Matching requests</h1>
        <p class="lead">
          {siteName} helps people describe what they want — lending, borrowing, or profit-sharing collaboration — and then
          connect with others who match the same terms and risk comfort. This service provides <strong>general educational guidance</strong>
          and introductions — not regulated financial advice.
        </p>

        <div class="callout" role="note" aria-label="Important notice">
          <strong>Important:</strong> AmicBridge does not manage money, hold funds, execute trades, or guarantee returns.
          Any agreement happens privately between participants. Lending and investing involve risk. Consider professional advice where appropriate.
        </div>

        <div class="actions">
          <a class="btn primary" href="#request">Send a request</a>
          <a class="btn" href="#how">How it works</a>
        </div>

        <ul class="bullets">
          <li><b>Simple:</b> goal, timeline, and risk comfort</li>
          <li><b>Private:</b> share only what is needed</li>
          <li><b>Clear:</b> structured next steps and introductions</li>
        </ul>
      </section>

      <section class="anchor" id="about">
        <h2>About</h2>
        <p>
          Many people want to lend, borrow, or collaborate on investments, but get overwhelmed by noise, hype, and unclear information.
          AmicBridge is built to make the first step boring-in-a-good-way: define what you want, define what you accept,
          and connect with people looking for the same kind of deal.
        </p>
        <p>
          We focus on being a <strong>bridge</strong>: coordination, matching, and clarity. We do not take custody of funds.
          We do not promise outcomes. We help people find each other and document terms clearly so participants can make their own decisions.
        </p>
      </section>

      <section class="anchor" id="how">
        <h2>How it works</h2>

        <div class="grid">
          <div class="card">
            <p>
              AmicBridge is a matching layer. You submit a request describing your intent and terms. If there’s a match,
              you can communicate directly and decide whether to proceed.
            </p>

            <div class="steps">
              <div class="step">
                <div class="kicker">Step 1</div>
                <strong>Describe your request</strong>
                <div class="muted">Choose lending, borrowing, or profit-sharing, then specify amount, timeline, and risk comfort.</div>
              </div>
              <div class="step">
                <div class="kicker">Step 2</div>
                <strong>We match by terms</strong>
                <div class="muted">We look for compatibility on structure, expectations, and basic fit — not hype.</div>
              </div>
              <div class="step">
                <div class="kicker">Step 3</div>
                <strong>You decide privately</strong>
                <div class="muted">If you choose to proceed, you do it directly with the other party. AmicBridge does not hold funds.</div>
              </div>
              <div class="step">
                <div class="kicker">Step 4</div>
                <strong>Clear documentation</strong>
                <div class="muted">Use simple written terms (and professional help if needed). Clarity reduces drama.</div>
              </div>
            </div>
          </div>

          <div class="card">
            <h3 style="margin:0 0 10px 0; font-size:18px;">Supported request types</h3>
            <ul class="bullets" style="margin-top:0;">
              <li><b>Lend:</b> fixed terms (amount, duration, repayment structure)</li>
              <li><b>Borrow:</b> timeline, purpose, and repayment plan</li>
              <li><b>Profit-sharing:</b> collaboration where returns vary and risk is explicit</li>
            </ul>

            <div class="spacer"></div>

            <h3 style="margin:0 0 10px 0; font-size:18px;">What we do not do</h3>
            <ul class="bullets" style="margin-top:0;">
              <li>We don’t hold or move money</li>
              <li>We don’t guarantee results or returns</li>
              <li>We don’t provide personal financial advice</li>
            </ul>
          </div>
        </div>
      </section>

      <section class="anchor" id="request">
        <h2>Send a request</h2>
        <p>
          Fill in the form below. This goes to AmicBridge via Formspree. Share only what is necessary.
        </p>

        <div class="grid">
          <div class="card">
            <form action={formspreeAction} method="POST">
              <div class="form-row">
                <div>
                  <label for="name">Your name</label>
                  <input id="name" name="name" autocomplete="name" required />
                </div>
                <div>
                  <label for="email">Email</label>
                  <input id="email" name="email" type="email" autocomplete="email" required />
                </div>
              </div>

              <div class="form-row">
                <div>
                  <label for="type">Request type</label>
                  <select id="type" name="request_type" required>
                    <option value="">Select…</option>
                    <option value="lend">I want to lend</option>
                    <option value="borrow">I want to borrow</option>
                    <option value="profit-sharing">I want profit-sharing collaboration</option>
                  </select>
                </div>
                <div>
                  <label for="amount">Amount (optional)</label>
                  <input id="amount" name="amount" placeholder="e.g., 10,000 NOK" />
                </div>
              </div>

              <div class="form-row">
                <div>
                  <label for="timeline">Timeline</label>
                  <input id="timeline" name="timeline" placeholder="e.g., 3–6 months" required />
                </div>
                <div>
                  <label for="risk">Risk comfort</label>
                  <select id="risk" name="risk_comfort" required>
                    <option value="">Select…</option>
                    <option value="low">Low</option>
                    <option value="medium">Medium</option>
                    <option value="high">High</option>
                  </select>
                </div>
              </div>

              <div>
                <label for="details">Details</label>
                <textarea
                  id="details"
                  name="details"
                  placeholder="Describe your request: terms, expectations, repayment plan (if borrowing), or collaboration idea (if profit-sharing)."
                  required
                ></textarea>
              </div>

              <!-- helpful metadata -->
              <input type="hidden" name="_subject" value="AmicBridge — New request" />

              <button class="btn primary" type="submit">Submit request</button>

              <p style="margin:8px 0 0 0; color: var(--muted); font-size: 13px;">
                By submitting, you confirm you understand AmicBridge does not hold funds, provide advice, or guarantee outcomes.
              </p>
            </form>
          </div>

          <div class="card">
            <h3 style="margin:0 0 10px 0; font-size:18px;">Tips for a good request</h3>
            <ul class="bullets" style="margin-top:0;">
              <li><b>Be specific:</b> amount, timeline, and what “success” looks like</li>
              <li><b>Be realistic:</b> higher returns usually mean higher risk</li>
              <li><b>Be safe:</b> don’t share sensitive IDs or passwords</li>
              <li><b>Be clear:</b> written terms beat misunderstandings</li>
            </ul>

            <div class="spacer"></div>

            <div class="callout">
              <strong>Note:</strong> Profit-sharing arrangements are risk-bearing and returns are not guaranteed.
              Make sure both sides understand the downside, not just the upside.
            </div>
          </div>
        </div>
      </section>

      <section class="anchor" id="contact">
        <h2>Contact</h2>
        <p>For questions, partnership inquiries, or support:</p>

        <div class="card">
          <p style="margin:0 0 8px 0;"><strong>CEO:</strong> Habbitx</p>
          <p style="margin:0 0 8px 0;">
            <strong>Email:</strong> <a href="mailto:Robertamicolsen@gmail.com">Robertamicolsen@gmail.com</a>
          </p>
          <p style="margin:0;"><strong>Phone:</strong> <a href="tel:92928373">92928373</a></p>
        </div>
      </section>
    </main>

    <footer>
      <div class="container">
        <p class="fineprint">
          <strong>Disclaimer:</strong> AmicBridge is an introduction and information platform. We do not hold custody of funds,
          execute payments, or provide personalized financial advice. Any decisions and agreements are made independently by the parties involved.
          Lending and investing carry risk, including potential loss of capital.
        </p>
        <p style="margin:10px 0 0 0;">© {new Date().getFullYear()} {siteName}. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
